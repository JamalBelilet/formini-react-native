# NativeBase
Using NativeBase's components is almost identical
to `react-native` components

1. import the ui components
```JSX
import {
  Container,
  Header,
  Title,
  // ...
} from 'native-base'
```

> the most used components are:
Container, Header, Title, Body, Icon, Text, Button, Card, Form, Input...
